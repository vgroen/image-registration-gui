from PyQt5.QtGui import QColor, QPalette

Dark = QPalette()
Dark.setColor(QPalette.Text, QColor(236, 240, 241, 255))
Dark.setColor(QPalette.Window, QColor(44, 62, 80, 255))


Light = QPalette()
Light.setColor(QPalette.Text, QColor(0, 0, 0, 255))
